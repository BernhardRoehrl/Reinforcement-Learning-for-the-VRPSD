# Append the df to the existing Excel
# row_counter = 6
# df = df.reset_index()
# lines = df.values.tolist()
# worksheet.write_row(5, 15, df.columns)
# for row in lines:
# worksheet.write_row(row_counter, 15, row)
# row_counter += 1